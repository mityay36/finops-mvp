# kubectl create secret generic finops-redis-secret -n finops \
#   --from-literal=REDIS_PASSWORD="$(openssl rand -base64 24 | tr -d '/+=')"
apiVersion: v1
kind: Secret
metadata:
  name: finops-redis-secret
  namespace: finops
type: Opaque
stringData:
  REDIS_PASSWORD: CHANGE_ME
