apiVersion: v1
kind: Service
metadata:
  name: finops-frontend
  namespace: finops
  labels:
    app: finops-frontend
spec:
  selector:
    app: finops-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
