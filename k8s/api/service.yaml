apiVersion: v1
kind: Service
metadata:
  name: finops-api
  namespace: finops
  labels:
    app: finops-api
spec:
  selector:
    app: finops-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
