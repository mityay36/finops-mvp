extends: default
rules:
  line-length:
    max: 150
  truthy:
    allowed-values: ['true', 'false', 'yes', 'no']
  comments:
    min-spaces-from-content: 1
  braces:
    max-spaces-inside: 1
ignore: |
  k8s/dashboards/*.yaml
